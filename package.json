{
  "name": "altimi-content-pipeline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "curl -s -X POST http://localhost:8787/articles/seed | jq",
    "status": "curl -s http://localhost:8787/articles | jq"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
